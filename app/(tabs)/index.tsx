import { View } from 'react-native';

/**
 * ARCHIVO DESACTIVADO
 * Se ha movido la lógica a app/index.tsx para evitar rutas duplicadas.
 * Renderizamos un View vacío para evitar el error 'Something went wrong' de Expo.
 */
export default function Unused() {
  return <View style={{ flex: 1, backgroundColor: '#fff' }} />;
}
